# Firefox extensions

Firefox has no CLI install and extensions live inside a per-machine, randomly
named profile directory (mixed with cache/history/cookies), so there's
nothing clean to symlink. Reinstall manually from addons.mozilla.org:

- Bitwarden Password Manager
- uBlock Origin
- Vimium
- Prevent Duplicate Tabs
